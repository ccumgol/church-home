# ══════════════════════════════════════════════════════════════════════════════
# 교회 홈페이지 템플릿 설정 (church)
# 이 블록은 HugoBlox 표준 설정이 아니라, 본 교회 템플릿에서 추가한 항목입니다.
# ══════════════════════════════════════════════════════════════════════════════
church:
  # ────────────────────────────────────────────────────────────────────────────
  # 구조 검사(빌드 가드)
  #   빌드할 때 '빌드는 성공하는데 기능만 사라지는' 상태를 찾아 로그로 알려 줍니다.
  #   검사 항목과 한계는 docs/manual.md 9절을 보세요.
  #
  #   false (기본) : 경고만 남기고 빌드는 계속합니다. 배포가 멈추지 않습니다.
  #   true         : '심각' 항목이 있으면 빌드를 실패시킵니다.
  #                  잘못된 상태가 배포되는 것을 막고 싶을 때(주로 CI) 켜세요.
  # ────────────────────────────────────────────────────────────────────────────
  guard_strict: false

  # ────────────────────────────────────────────────────────────────────────────
  # 첫 화면(히어로) 배너 스타일과 캐러셀 전환 간격은
  #   ▶ Admin(/admin/) → "⚙️ 사이트 설정" → "첫 화면 배너"
  # 에서 바꿉니다. 실제 값은 data/settings.yaml 에 저장됩니다.
  #
  #   hero_style: ticker   → 배경 사진 한 장 + 최근소식 티커
  #   hero_style: carousel → 여러 장이 자동으로 넘어가는 슬라이드
  #
  # 여기(params.yaml)에 hero_style / carousel_interval_seconds 를 적으면
  # data/settings.yaml 이 없을 때 쓰이는 예비값이 됩니다. 평소에는 비워 두고
  # Admin 에서 관리하세요. (해석 규칙: layouts/_partials/church/settings.html)
  # ────────────────────────────────────────────────────────────────────────────

  # ────────────────────────────────────────────────────────────────────────────
  # 교회 기본 정보 (푸터·안내 문구 등에서 참조할 수 있는 단일 출처)
  # ────────────────────────────────────────────────────────────────────────────
  contact:
    address: "123 Main St, Palisades Park, NJ 07650"
    phone: "(201) 555-0100"
    email: "contact@samplechurch.org"

# ══════════════════════════════════════════════════════════════════════════════
# HUGO BLOX CONFIGURATION
# Schema: hugoblox.com/schema/v2
# Tutorial: https://docs.hugoblox.com/tutorial/
# Documentation: https://docs.hugoblox.com/
# ══════════════════════════════════════════════════════════════════════════════
hugoblox:
  schema: "2.0"

  # ────────────────────────────────────────────────────────────────────────────
  # IDENTITY
  # Site-level branding (for people/profiles, use the author system in content/authors/)
  # ────────────────────────────────────────────────────────────────────────────
  identity:
    # Site name - shown in navbar, footer, copyright, and browser title by default
    name: "샘플교회"
    # Legal entity for copyright notices (defaults to name if not set)
    organization: "샘플교회"
    # Schema.org type for structured data
    type: organization # person | organization | local_business
    # Short tagline displayed on your site
    tagline: "하나님의 말씀과 은혜로 새로워지는 교회"
    # Site meta description for search engines and social sharing
    description: "샘플교회 홈페이지 템플릿입니다. 예배 및 모임, 주일설교, 교육 및 양육 안내."
    # 푸터에 표시할 소셜 링크.
    # 교회에서 실제로 운영하는 채널만 남기세요.
    # (템플릿 기본값이던 X·GitHub·Discord·LinkedIn 은 교회와 무관해 제거했습니다)
    social:
      links:
        - icon: brands/youtube
          url: https://youtube.com/

  # ────────────────────────────────────────────────────────────────────────────
  # THEME
  # Color and visual design system
  # ────────────────────────────────────────────────────────────────────────────
  theme:
    # Mode: 'light', 'dark', or 'system' (follows OS preference)
    mode: system
    # 색은 data/themes/church-warm.yaml 에서 한 번에 관리합니다.
    # 아래 colors/surfaces 는 비워 두세요. 값을 넣으면 테마팩을 덮어써서
    # 예전처럼 색이 두 갈래로 갈라집니다.
    pack: "church-warm"
    colors:
      primary: ""
      secondary: ""
      neutral: ""
    colors_light: {}
    colors_dark: {}
    surfaces:
      background: ""
      foreground: ""
      header:
        background: ""
        foreground: ""
      footer:
        background: ""
        foreground: ""

  # ────────────────────────────────────────────────────────────────────────────
  # TYPOGRAPHY
  # Font and text sizing
  # ────────────────────────────────────────────────────────────────────────────
  typography:
    # Theme pack: name from data/fonts/
    pack: "geometric"

  # ────────────────────────────────────────────────────────────────────────────
  # LAYOUT
  # Spacing and shape tokens
  # ────────────────────────────────────────────────────────────────────────────
  layout:
    # Border radius: 'none', 'sm', 'md', 'lg', or 'full'
    radius: "md"
    # Spacing density: 'compact', 'comfortable', or 'spacious'
    spacing: "spacious"
    # Avatar shape in author profiles
    avatar_shape: circle # circle | square | rounded

  # ────────────────────────────────────────────────────────────────────────────
  # HEADER
  # Site header and navigation bar
  # ────────────────────────────────────────────────────────────────────────────
  header:
    enable: true
    # Header style variant
    block: navbar # navbar | navbar-simple
    # Stick to top on scroll
    sticky: true
    # Show logo in navbar
    show_logo: false
    # Show site title text in navbar
    show_title: true
    # Override navbar title (defaults to identity.name)
    title: ""
    # Menu alignment
    align: center # left | center | right
    # Feature toggles
    search: true
    theme_toggle: true
    theme_picker: false
    language_switcher: true
    # Call-to-action button
    cta:
      enable: false
      text: ""
      url: ""

  # ────────────────────────────────────────────────────────────────────────────
  # FOOTER
  # Site footer
  # ────────────────────────────────────────────────────────────────────────────
  footer:
    # Footer style variant
    style: columns # minimal | columns | centered
    # 템플릿 기본 로고(assets/media/logo.svg)가 "YOUR LOGO" 플레이스홀더라 끕니다.
    # 교회 로고 파일을 넣으면 true 로 바꾸세요.
    show_logo: false
    show_title: true
    show_tagline: true
    # Override footer title (defaults to identity.name)
    title: ""
    language_switcher: true
    # Show social icons from identity.social.links
    show_social: true
    # Custom footer text (HTML supported)
    text: ""

  # ────────────────────────────────────────────────────────────────────────────
  # COPYRIGHT
  # Legal notices and licensing
  # ────────────────────────────────────────────────────────────────────────────
  copyright:
    # Copyright notice - supports {year}, {name}, {license} placeholders
    notice: "© {year} {name}."
    license:
      type: custom # cc | custom
      allow_derivatives: false
      share_alike: true
      allow_commercial: false

  # ────────────────────────────────────────────────────────────────────────────
  # SEO
  # Search engine optimization - overrides and technical settings
  # ────────────────────────────────────────────────────────────────────────────
  seo:
    # Browser tab title override (defaults to identity.name if not set)
    title: ""
    # For local_business identity type only
    location:
      address:
        street: ""
        city: ""
        region: ""
        postal_code: ""
        country: ""
      coordinates:
        latitude: null
        longitude: null
      phone: ""
    # AI/LLM crawler guidance (llms.txt)
    ai:
      allow:
        - /
      disallow: []
      note: "Guidance for AI crawlers - customize to match your public content."
      contact: ""

  # ────────────────────────────────────────────────────────────────────────────
  # CONTENT
  # Content rendering and behavior
  # ────────────────────────────────────────────────────────────────────────────
  content:
    # Math rendering
    math:
      enable: false
    # Table of contents
    toc:
      enable: true
    # Reading time estimates
    reading_time:
      enable: true
    # Academic citations
    citations:
      style: apa # apa | mla | chicago | ieee

  # ────────────────────────────────────────────────────────────────────────────
  # SEARCH
  # Site search
  # ────────────────────────────────────────────────────────────────────────────
  search:
    enable: true
    # Trending/suggested search terms
    suggestions: []
    # Quick action shortcuts in search modal
    quick_actions: []

  # ────────────────────────────────────────────────────────────────────────────
  # COMMENTS
  # User comments system
  # ────────────────────────────────────────────────────────────────────────────
  comments:
    enable: false
    provider: "" # giscus | disqus
    giscus:
      repo: ""
      repo_id: ""
      category: ""
      category_id: ""
    disqus:
      shortname: ""

  # ────────────────────────────────────────────────────────────────────────────
  # ANALYTICS
  # Traffic and behavior analytics
  # ────────────────────────────────────────────────────────────────────────────
  analytics:
    google:
      measurement_id: "" # Google Analytics 4: G-XXXXXXXXXX
    google_tag_manager:
      container_id: "" # GTM-XXXXXXX
    plausible:
      domain: ""
    fathom:
      site_id: ""
    pirsch:
      site_id: ""
    clarity:
      project_id: ""
    baidu:
      site_id: ""

  # ────────────────────────────────────────────────────────────────────────────
  # VERIFICATION
  # Search engine site verification codes
  # ────────────────────────────────────────────────────────────────────────────
  verification:
    google: ""
    bing: ""
    baidu: ""
    yandex: ""
    pinterest: ""
    naver: ""

  # ────────────────────────────────────────────────────────────────────────────
  # REPOSITORY
  # Source code repository for "Edit this page" links
  # ────────────────────────────────────────────────────────────────────────────
  repository:
    enable: false
    url: "" # e.g., https://github.com/username/repo
    branch: main
    content_dir: content

  # ────────────────────────────────────────────────────────────────────────────
  # LOCALE
  # Regional formatting preferences
  # ────────────────────────────────────────────────────────────────────────────
  locale:
    date_format: "Jan 2, 2006" # Go time format
    time_format: "3:04 PM"
    address_format: en-us

  # ────────────────────────────────────────────────────────────────────────────
  # SECURITY
  # Security headers and policies (requires Netlify integration)
  # ────────────────────────────────────────────────────────────────────────────
  security:
    csp:
      policy: ""
      report_only: false
    frame_options: allow # deny | sameorigin | allow - allow enables iframe embedding
    permissions_policy: ""

  # ────────────────────────────────────────────────────────────────────────────
  # PRIVACY
  # Privacy and compliance features
  # ────────────────────────────────────────────────────────────────────────────
  privacy:
    enable: false
    anonymize_analytics: true

  # ────────────────────────────────────────────────────────────────────────────
  # DEBUG
  # Development and debugging (hidden in production)
  # ────────────────────────────────────────────────────────────────────────────
  debug:
    enable: false
    hud:
      position: bottom-left # top-left | top-right | bottom-left | bottom-right
      opacity: 1.0
    export_logs: true

  # ────────────────────────────────────────────────────────────────────────────
  # PREMIUM
  # Premium features and creator program (affiliates)
  # ────────────────────────────────────────────────────────────────────────────
  pro:
    # Hide "Powered by HugoBlox" attribution
    # Requires: HugoBlox Premium to support HugoBlox via alternative means
    # Note: Even with Premium, you can keep this false to support open source!
    # Get Premium: https://hugoblox.com/premium
    hide_attribution: false

    # Affiliate referral code for rewards program
    # Join affiliate program: https://hugoblox.com/affiliates
    affiliate_code: ""
